---
const isActive = (href: string) => Astro.url.pathname === href;
---

<header class="header reveal visible" role="banner">
  <div class="header-inner container">
    <a href="/" class="logo" itemprop="url">
      <span class="brand-mark" aria-hidden="true">$</span>
      <span class="brand-copy">
        <span class="brand-name" itemprop="name">abx</span>
        <span class="brand-role">Software Engineer</span>
      </span>
    </a>

    <nav class="nav-desktop" role="navigation" aria-label="Main navigation">
      <a href="/" class:list={["nav-link", { active: isActive("/") }]} itemprop="url">home</a>
      <a href="/resume" class:list={["nav-link", { active: isActive("/resume") }]} itemprop="url">resume</a>
      <a href="/contact" class:list={["nav-link", { active: isActive("/contact") }]} itemprop="url">contact</a>
    </nav>

    <div class="header-actions">
      <span class="header-side-note mono">connect</span>
      <div class="nav-social" role="navigation" aria-label="Social links">
        <a href="https://github.com/AbiXnash" target="_blank" rel="me noreferrer noopener" class="social-link" aria-label="GitHub profile" itemprop="sameAs">
          <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
            <path d="M12 2C6.48 2 2 6.58 2 12.28c0 4.52 2.87 8.36 6.84 9.72.5.1.68-.22.68-.48v-1.7c-2.78.62-3.37-1.4-3.37-1.4-.45-1.16-1.1-1.47-1.1-1.47-.9-.63.07-.62.07-.62 1 .07 1.53 1.05 1.53 1.05.9 1.56 2.36 1.11 2.94.85.09-.67.35-1.11.63-1.37-2.22-.26-4.56-1.14-4.56-5.05 0-1.12.39-2.03 1.03-2.75-.1-.26-.45-1.32.1-2.75 0 0 .84-.28 2.75 1.05a9.4 9.4 0 0 1 5 0c1.91-1.33 2.75-1.05 2.75-1.05.55 1.43.2 2.49.1 2.75.64.72 1.03 1.63 1.03 2.75 0 3.92-2.34 4.78-4.57 5.03.36.32.68.94.68 1.9v2.82c0 .26.18.58.69.48A10.3 10.3 0 0 0 22 12.28C22 6.58 17.52 2 12 2z"/>
          </svg>
        </a>
        <a href="https://linkedin.com/in/abinash-selvarasu" target="_blank" rel="me noreferrer noopener" class="social-link" aria-label="LinkedIn profile" itemprop="sameAs">
          <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
            <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
          </svg>
        </a>
      </div>

      <button class="mobile-menu-btn" aria-label="Toggle menu" aria-expanded="false" aria-controls="mobileNav" id="mobileMenuBtn">
        <span class="mobile-menu-text">menu</span>
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
          <line x1="3" y1="12" x2="21" y2="12"/>
          <line x1="3" y1="6" x2="21" y2="6"/>
          <line x1="3" y1="18" x2="21" y2="18"/>
        </svg>
      </button>
    </div>
  </div>
</header>

<div class="mobile-nav-overlay" id="mobileOverlay" aria-hidden="true"></div>

<nav class="mobile-nav" id="mobileNav" role="navigation" aria-label="Mobile navigation" aria-hidden="true">
  <div class="mobile-nav-header">
    <div class="mobile-nav-intro">
      <span class="mobile-nav-brand">
        <span class="brand-mark" aria-hidden="true">$</span>
        <span class="brand-name">abx</span>
      </span>
      <p class="mobile-nav-label">Navigate the site</p>
    </div>
    <button class="mobile-nav-close" id="mobileNavClose" aria-label="Close menu">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
        <line x1="18" y1="6" x2="6" y2="18"/>
        <line x1="6" y1="6" x2="18" y2="18"/>
      </svg>
    </button>
  </div>
  
  <div class="mobile-nav-links">
    <a href="/" class:list={["mobile-nav-link", { active: isActive("/") }]}>
      <span class="link-icon">~/</span>
      <span>home</span>
    </a>
    <a href="/resume" class:list={["mobile-nav-link", { active: isActive("/resume") }]}>
      <span class="link-icon">~/</span>
      <span>resume</span>
    </a>
    <a href="/contact" class:list={["mobile-nav-link", { active: isActive("/contact") }]}>
      <span class="link-icon">~/</span>
      <span>contact</span>
    </a>
  </div>

  <div class="mobile-nav-footer">
    <a href="https://github.com/AbiXnash" target="_blank" rel="noreferrer" class="mobile-nav-social">
      <svg viewBox="0 0 24 24" fill="currentColor">
        <path d="M12 2C6.48 2 2 6.58 2 12.28c0 4.52 2.87 8.36 6.84 9.72.5.1.68-.22.68-.48v-1.7c-2.78.62-3.37-1.4-3.37-1.4-.45-1.16-1.1-1.47-1.1-1.47-.9-.63.07-.62.07-.62 1 .07 1.53 1.05 1.53 1.05.9 1.56 2.36 1.11 2.94.85.09-.67.35-1.11.63-1.37-2.22-.26-4.56-1.14-4.56-5.05 0-1.12.39-2.03 1.03-2.75-.1-.26-.45-1.32.1-2.75 0 0 .84-.28 2.75 1.05a9.4 9.4 0 0 1 5 0c1.91-1.33 2.75-1.05 2.75-1.05.55 1.43.2 2.49.1 2.75.64.72 1.03 1.63 1.03 2.75 0 3.92-2.34 4.78-4.57 5.03.36.32.68.94.68 1.9v2.82c0 .26.18.58.69.48A10.3 10.3 0 0 0 22 12.28C22 6.58 17.52 2 12 2z"/>
      </svg>
      <span>@AbiXnash</span>
    </a>
    <a href="https://linkedin.com/in/abinash-selvarasu" target="_blank" rel="noreferrer" class="mobile-nav-social">
      <svg viewBox="0 0 24 24" fill="currentColor">
        <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
      </svg>
      <span class="social-name">@abinash-selvarasu</span>
    </a>
  </div>
</nav>

<style>
  .header {
    position: sticky;
    top: 0;
    z-index: 100;
    background: rgba(12, 12, 12, 0.72);
    backdrop-filter: blur(16px);
    -webkit-backdrop-filter: blur(16px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.06);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.12);
    transition:
      background 180ms ease,
      border-color 180ms ease,
      box-shadow 180ms ease;
  }

  .header-inner {
    display: flex;
    position: relative;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 0.85rem 1rem;
    max-width: 1120px;
    margin: 0 auto;
  }

  @media (min-width: 640px) {
    .header-inner {
      padding: 1rem 1.5rem;
    }
  }

  @media (min-width: 768px) {
    .header-inner {
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
      align-items: center;
      gap: 1.25rem;
      padding-top: 1.05rem;
      padding-bottom: 1.05rem;
    }
  }

  .logo {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    min-width: 0;
    padding: 0.2rem 0;
    transition: opacity 160ms ease, transform 160ms ease;
  }

  .logo:hover {
    opacity: 0.9;
    transform: translateY(-1px);
  }

  .nav-desktop {
    display: none;
    align-items: center;
    gap: 0.35rem;
    padding: 0.35rem;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid rgba(255, 255, 255, 0.06);
  }

  @media (min-width: 768px) {
    .nav-desktop {
      display: flex;
      justify-self: center;
    }
  }

  .nav-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 2.25rem;
    padding: 0.45rem 0.9rem;
    border-radius: 999px;
    font-size: 0.875rem;
    color: var(--text-muted);
    transition: color 160ms ease, background 160ms ease, transform 160ms ease;
  }

  .nav-link:hover,
  .nav-link.active {
    color: var(--text);
    background: rgba(255, 255, 255, 0.04);
  }

  .nav-link.active {
    color: #111;
    background: linear-gradient(135deg, var(--accent), #ff8a5c);
    box-shadow: 0 8px 18px rgba(255, 107, 53, 0.18);
    animation: navActivePulse 420ms ease-out;
  }

  .nav-social {
    display: none;
    gap: 0.5rem;
  }

  @media (min-width: 768px) {
    .nav-social {
      display: flex;
    }
  }

  .header-actions {
    display: flex;
    align-items: center;
    gap: 0.65rem;
    margin-left: auto;
  }

  @media (min-width: 768px) {
    .header-actions {
      justify-self: end;
      margin-left: 0;
    }
  }

  .brand-mark {
    font-family: "JetBrains Mono", monospace;
    color: var(--accent);
    font-weight: 500;
    font-size: 0.9rem;
    text-shadow: 0 0 10px rgba(255, 107, 53, 0.5);
  }

  .brand-name {
    font-family: "JetBrains Mono", monospace;
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--text);
  }

  .brand-copy {
    display: grid;
    gap: 0.1rem;
    min-width: 0;
  }

  .brand-role {
    font-size: 0.7rem;
    color: var(--text-dim);
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .header-side-note {
    display: none;
    font-size: 0.68rem;
    color: var(--text-dim);
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  @media (min-width: 960px) {
    .header-side-note {
      display: inline-flex;
      align-items: center;
    }
  }

  .social-link,
  .mobile-nav-social {
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--text-muted);
    background: rgba(15, 15, 15, 0.6);
    border: 1px solid rgba(255, 255, 255, 0.06);
    transition:
      color 180ms ease,
      border-color 180ms ease,
      background 180ms ease,
      transform 180ms ease;
  }

  .social-link:hover,
  .mobile-nav-social:hover {
    color: var(--text);
    border-color: rgba(255, 107, 53, 0.35);
    background: rgba(255, 107, 53, 0.08);
  }

  .social-link {
    width: 36px;
    height: 36px;
    border-radius: 10px;
  }

  .social-link:hover {
    transform: translateY(-2px);
  }

  .social-link svg {
    width: 20px;
    height: 20px;
  }

  .mobile-menu-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    min-width: 40px;
    height: 40px;
    background: rgba(23, 23, 23, 0.6);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 999px;
    cursor: pointer;
    transition: all 150ms ease;
    padding: 0 0.85rem;
  }

  @media (min-width: 768px) {
    .mobile-menu-btn {
      display: none;
    }
  }

  .mobile-menu-btn:hover {
    border-color: var(--accent);
    background: rgba(255, 107, 53, 0.1);
  }

  .mobile-menu-text {
    font-family: "JetBrains Mono", monospace;
    font-size: 0.68rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--text-muted);
  }

  .mobile-menu-btn svg {
    width: 18px;
    height: 18px;
    color: var(--text-muted);
  }

  .mobile-nav-overlay {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.6);
    backdrop-filter: blur(4px);
    z-index: 200;
    opacity: 0;
    transition: opacity 200ms ease;
  }

  .mobile-nav-overlay.open {
    display: block;
    opacity: 1;
  }

  .mobile-nav {
    position: fixed;
    top: 1rem;
    left: 1rem;
    right: 1rem;
    background: rgba(18, 18, 18, 0.94);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 24px;
    padding: 1.15rem 1rem 1rem;
    z-index: 201;
    transform: translateY(-1rem);
    opacity: 0;
    pointer-events: none;
    transition:
      transform 240ms cubic-bezier(0.4, 0, 0.2, 1),
      opacity 240ms ease;
    max-height: calc(100vh - 2rem);
    overflow-y: auto;
    box-shadow: 0 24px 48px rgba(0, 0, 0, 0.34);
  }

  .mobile-nav.open {
    transform: translateY(0);
    opacity: 1;
    pointer-events: auto;
  }

  .mobile-nav-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 0 0 1rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    margin-bottom: 0.75rem;
  }

  .mobile-nav-intro {
    display: grid;
    gap: 0.45rem;
    min-width: 0;
  }

  .mobile-nav-brand {
    display: flex;
    align-items: center;
    gap: 0.35rem;
  }

  .mobile-nav-label {
    color: var(--text-muted);
    font-size: 0.85rem;
    line-height: 1.5;
  }

  .mobile-nav-close {
    width: 40px;
    height: 40px;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    background: rgba(15, 15, 15, 0.8);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 50%;
    cursor: pointer;
    transition: all 150ms ease;
  }

  .mobile-nav-close:hover {
    border-color: var(--accent);
    background: rgba(255, 107, 53, 0.1);
  }

  .mobile-nav-close svg {
    width: 20px;
    height: 20px;
    color: var(--text-muted);
    stroke-width: 2;
  }

  .mobile-nav-links {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    padding-top: 0.35rem;
  }

  .mobile-nav-link {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.75rem;
    padding: 1rem 1rem;
    background: rgba(15, 15, 15, 0.6);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 14px;
    font-size: 0.96rem;
    color: var(--text-muted);
    transition: all 180ms ease;
  }

  .mobile-nav-link:hover,
  .mobile-nav-link.active {
    border-color: rgba(255, 107, 53, 0.4);
    color: var(--text);
    background: rgba(255, 107, 53, 0.08);
  }

  .mobile-nav-link.active {
    color: var(--accent);
  }

  .mobile-nav-link::after {
    content: '->';
    font-family: "JetBrains Mono", monospace;
    font-size: 0.72rem;
    color: rgba(255, 255, 255, 0.28);
    transition: color 160ms ease, transform 160ms ease;
  }

  .mobile-nav-link:hover::after,
  .mobile-nav-link.active::after {
    color: var(--accent);
    transform: translateX(2px);
  }

  .mobile-nav-link.active .link-icon {
    color: var(--accent);
  }

  .link-icon {
    font-family: "JetBrains Mono", monospace;
    font-size: 0.75rem;
    color: var(--text-dim);
  }

  .mobile-nav-footer {
    display: flex;
    gap: 0.65rem;
    margin-top: 1.1rem;
    padding-top: 0.9rem;
    border-top: 1px solid rgba(255, 255, 255, 0.06);
  }

  .mobile-nav-social {
    flex: 1;
    gap: 0.5rem;
    padding: 0.875rem 1.25rem;
    border-radius: 10px;
    font-size: 0.8125rem;
    min-width: 0;
  }

  .mobile-nav-social svg {
    width: 18px;
    height: 18px;
    flex-shrink: 0;
  }

  .social-name {
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  @media (max-width: 420px) {
    .mobile-nav {
      left: 0.75rem;
      right: 0.75rem;
      top: 0.75rem;
      padding: 1rem 0.85rem 0.9rem;
    }

    .mobile-nav-footer {
      flex-direction: column;
    }
  }

  @keyframes navActivePulse {
    0% {
      box-shadow: 0 0 0 rgba(255, 107, 53, 0);
    }
    100% {
      box-shadow: 0 8px 18px rgba(255, 107, 53, 0.18);
    }
  }
</style>

<script>
  const btn = document.getElementById('mobileMenuBtn');
  const nav = document.getElementById('mobileNav');
  const overlay = document.getElementById('mobileOverlay');
  const closeBtn = document.getElementById('mobileNavClose');
  
  function openNav() {
    nav?.classList.add('open');
    overlay?.classList.add('open');
    nav?.setAttribute('aria-hidden', 'false');
    btn?.setAttribute('aria-expanded', 'true');
    document.body.style.overflow = 'hidden';
  }
  
  function closeNav() {
    nav?.classList.remove('open');
    overlay?.classList.remove('open');
    nav?.setAttribute('aria-hidden', 'true');
    btn?.setAttribute('aria-expanded', 'false');
    document.body.style.overflow = '';
  }
  
  btn?.addEventListener('click', () => {
    if (nav?.classList.contains('open')) {
      closeNav();
      return;
    }

    openNav();
  });
  closeBtn?.addEventListener('click', closeNav);
  overlay?.addEventListener('click', closeNav);
  document.addEventListener('keydown', (event) => {
    if (event.key === 'Escape' && nav?.classList.contains('open')) {
      closeNav();
    }
  });
  
  nav?.querySelectorAll('a').forEach(link => {
    link.addEventListener('click', closeNav);
  });
</script>
