<section class="contact-page container">
  <div class="contact-intro section-shell">
    <div class="contact-copy">
      <div class="contact-header">
        <span class="prompt">~/</span>
        <span>contact</span>
      </div>

      <h1 class="contact-title">Let's build something useful.</h1>

      <p class="contact-bio">
        If you have a product or system that needs strong backend engineering, send a short note.
        I’m most useful on payments, integration-heavy platforms, internal tooling, and systems that need reliability.
      </p>

      <div class="contact-points">
        <span class="pill">Reply in 24-48h</span>
        <span class="pill">Remote friendly</span>
        <span class="pill pill-accent">Open to consulting and product work</span>
      </div>
    </div>
  </div>

  <div class="contact-panel section-shell">
    <div class="contact-primary">
      <p class="section-kicker">Primary Contact</p>
      <h2 class="contact-section-title">Email is the best starting point.</h2>
      <p class="contact-section-copy">
        Include the problem, current stage, and what kind of help you need. I’ll reply with whether I’m a fit and how I’d approach it.
      </p>

      <a href="mailto:abinash@theabx.in" class="email-cta">
        <span class="email-cta-label">Email</span>
        <span class="email-cta-text">abinash@theabx.in</span>
      </a>
    </div>

    <div class="contact-secondary">
      <div class="contact-secondary-block">
        <p class="section-kicker">Good Fit</p>
        <ul class="fit-list">
          <li>payment and transaction flows</li>
          <li>backend architecture and APIs</li>
          <li>integration-heavy products</li>
          <li>internal platforms and tooling</li>
        </ul>
      </div>

      <div class="contact-secondary-block">
        <p class="section-kicker">Profiles</p>
        <div class="social-links">
          <a href="https://github.com/AbiXnash" target="_blank" rel="noreferrer" class="social-link">
            <div class="social-link-main">
              <span>GitHub</span>
              <span class="social-handle">@AbiXnash</span>
            </div>
            <span class="social-link-arrow">-></span>
          </a>
          <a href="https://linkedin.com/in/abinash-selvarasu" target="_blank" rel="noreferrer" class="social-link">
            <div class="social-link-main">
              <span>LinkedIn</span>
              <span class="social-handle">abinash-selvarasu</span>
            </div>
            <span class="social-link-arrow">-></span>
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .contact-page {
    padding: 3rem 0 1.5rem;
    display: grid;
    gap: 1.5rem;
  }

  @media (min-width: 900px) {
    .contact-page {
      padding: 4rem 0 3rem;
      gap: 1.75rem;
    }
  }

  .contact-intro {
    padding: 1.4rem;
  }

  .contact-copy {
    display: grid;
    gap: 1rem;
  }

  .contact-header {
    font-family: "JetBrains Mono", monospace;
    font-size: 0.8125rem;
    color: var(--text-muted);
  }

  .prompt {
    color: var(--accent);
  }

  .contact-title {
    font-size: clamp(2.15rem, 8vw, 3.8rem);
    font-weight: 700;
    line-height: 1.02;
    letter-spacing: -0.02em;
    margin: 0;
  }

  .contact-bio {
    font-size: 1.02rem;
    color: var(--text-muted);
    line-height: 1.7;
    max-width: 64ch;
  }

  .contact-points {
    display: flex;
    flex-wrap: wrap;
    gap: 0.65rem;
  }

  .contact-panel {
    display: grid;
    gap: 1.25rem;
    padding: 1.25rem;
  }

  @media (min-width: 900px) {
    .contact-panel {
      grid-template-columns: minmax(0, 1.15fr) minmax(280px, 0.85fr);
      align-items: start;
    }
  }

  .contact-primary,
  .contact-secondary-block {
    display: grid;
    gap: 1rem;
  }

  .contact-secondary {
    display: grid;
    gap: 1rem;
    align-content: start;
  }

  .contact-section-title {
    font-size: 1.15rem;
    font-weight: 650;
    color: var(--text);
  }

  .contact-section-copy {
    max-width: 52ch;
    color: var(--text-muted);
    font-size: 0.92rem;
    line-height: 1.65;
  }

  .email-cta {
    display: grid;
    gap: 0.25rem;
    padding: 1.15rem 1.2rem;
    border-radius: 18px;
    border: 1px solid rgba(255, 107, 53, 0.2);
    background:
      linear-gradient(135deg, rgba(255, 107, 53, 0.12), rgba(255, 255, 255, 0.02)),
      rgba(255, 255, 255, 0.01);
    transition:
      transform 180ms ease,
      border-color 180ms ease,
      background 180ms ease;
  }

  .email-cta:hover {
    transform: translateY(-2px);
    border-color: rgba(255, 107, 53, 0.35);
  }

  .email-cta-text {
    font-family: "JetBrains Mono", monospace;
    font-size: clamp(1rem, 3vw, 1.25rem);
    font-weight: 700;
    color: var(--text);
  }

  .email-cta-label {
    font-size: 0.76rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    color: var(--text-dim);
  }

  .fit-list {
    display: grid;
    gap: 0.7rem;
    list-style: none;
  }

  .fit-list li {
    padding-left: 1rem;
    position: relative;
    color: var(--text-muted);
    font-size: 0.88rem;
    line-height: 1.6;
  }

  .fit-list li::before {
    content: "";
    position: absolute;
    left: 0;
    top: 0.65rem;
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: var(--accent);
  }

  .social-links {
    display: grid;
    gap: 0.75rem;
  }

  .social-link {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.85rem;
    padding: 1rem 1rem;
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 16px;
    background: rgba(255, 255, 255, 0.02);
    transition:
      transform 180ms ease,
      border-color 180ms ease,
      background 180ms ease;
  }

  .social-link:hover {
    transform: translateY(-2px);
    border-color: rgba(255, 107, 53, 0.24);
    background: rgba(255, 107, 53, 0.05);
  }

  .social-link-main {
    display: grid;
    gap: 0.18rem;
    min-width: 0;
  }

  .social-link-main > span:first-child {
    font-size: 0.92rem;
    font-weight: 600;
    color: var(--text);
  }

  .social-handle {
    font-family: "JetBrains Mono", monospace;
    font-size: 0.72rem;
    color: var(--text-dim);
    line-height: 1.55;
  }

  .social-link-arrow {
    color: rgba(255, 255, 255, 0.28);
    font-family: "JetBrains Mono", monospace;
    font-size: 0.78rem;
    transition: transform 180ms ease, color 180ms ease;
  }

  .social-link:hover .social-link-arrow {
    color: var(--accent);
    transform: translateX(2px);
  }

  @media (max-width: 640px) {
    .contact-intro,
    .contact-panel {
      padding: 1rem;
    }

    .contact-section-copy {
      max-width: none;
    }

    .social-link {
      padding: 0.9rem;
    }
  }
</style>
