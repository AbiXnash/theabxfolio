<section
  class="mt-16 flex items-center bg-black px-4 py-16 font-mono text-white sm:mt-24 sm:px-6 md:mt-32"
>
  <div class="mx-auto w-full max-w-5xl">
    <!-- TERMINAL PROMPT -->
    <p class="text-sm text-green-400">$ whoami</p>

    <h1 class="mt-6 text-3xl font-bold tracking-tight sm:text-4xl md:text-5xl">
      Abinash S
    </h1>

    <p class="mt-2 text-sm text-white/60 sm:text-base">
      Software Engineer · Full-Stack · Systems-Focused
    </p>

    <!-- PROFILE -->
    <p class="mt-10 text-sm text-green-400">$ cat profile.txt</p>

    <p
      class="mt-4 max-w-3xl font-sans text-sm leading-relaxed text-white/75 sm:text-base"
    >
      I build performance-driven applications with a strong emphasis on clean
      architecture, scalability, and long-term maintainability. My work
      prioritizes clarity, reliability, and production readiness.
    </p>

    <!-- HELP / AFFORDANCE -->
    <p class="mt-6 text-xs text-white/40">
      Tip: click commands below to navigate
    </p>

    <!-- COMMAND LIST -->
    <div class="mt-8 space-y-4 text-sm sm:text-base">
      <!-- RESUME -->
      <div class="flex items-center gap-3">
        <span class="text-green-400">$</span>
        <a href="/resume" class="text-blue-400 transition hover:text-blue-300">
          open resume
        </a>
        <span class="hidden text-white/30 sm:inline">
          — detailed experience & projects
        </span>
      </div>

      <!-- SERVICE -->
      <div class="flex items-center gap-3">
        <span class="text-green-400">$</span>
        <a
          href="/services"
          class="text-green-400 transition hover:text-green-300"
        >
          view services
        </a>
        <span class="hidden text-white/30 sm:inline">
          — offerings & collaboration options
        </span>
      </div>

      <!-- CONTACT -->
      <div class="flex items-center gap-3">
        <span class="text-green-400">$</span>
        <a
          href="/contact"
          class="text-green-400 transition hover:text-green-300"
        >
          initiate contact
        </a>
        <span class="hidden text-white/30 sm:inline">
          — email & social links
        </span>
      </div>
    </div>

    <!-- FUN / HUMAN TOUCH -->
    <div class="mt-12 flex items-center gap-4">
      <button
        id="heartBtn"
        aria-label="like"
        class="flex items-center gap-2 rounded-md border border-white/10 px-3 py-1.5 text-white/60 transition hover:border-red-400/40 hover:text-red-400"
      >
        <span id="heartIcon">♡</span>
        <span id="heartCount" class="text-xs">0</span>
      </button>

      <p class="text-xs text-white/40">click if this made you smile</p>
    </div>

    <!-- FOOTER CONTEXT -->
    <p class="mt-10 text-xs text-white/30">
      This site is designed as a terminal-inspired interface for clarity and
      focus.
    </p>
  </div>
</section>

<!-- HEART SCRIPT -->
<script>
  document.addEventListener("DOMContentLoaded", () => {
    const btn = document.getElementById("heartBtn");
    const icon = document.getElementById("heartIcon");
    const countEl = document.getElementById("heartCount");

    let count = Number(localStorage.getItem("hero-hearts") || 0);
    countEl.textContent = count;

    btn.addEventListener("click", () => {
      count++;
      localStorage.setItem("hero-hearts", count);
      countEl.textContent = count;

      icon.textContent = "♥";
      btn.classList.add("text-red-400");

      setTimeout(() => {
        icon.textContent = "♡";
      }, 300);
    });
  });
</script>
