<section class="page-shell">
  <div class="page-inner hero-grid">
    <div class="hero-block">
      <span class="hero-kicker">$ whoami</span>
      <h1 class="hero-title">Abinash S</h1>
      <p class="hero-subtitle">
        Software Engineer focused on backend systems, dependable architectures,
        and production-grade delivery across banking, web, and research
        projects.
      </p>

      <div class="hero-actions">
        <a href="/resume" class="btn-primary">View Resume</a>
        <a href="/service" class="btn-ghost">Services</a>
        <a href="/contact" class="btn-ghost">Contact</a>
      </div>

      <div class="hero-tags">
        <span class="tag">Java · Spring Boot · Microservices</span>
        <span class="tag">Payments · Transaction Systems</span>
        <span class="tag">ML Research · Cybersecurity</span>
      </div>
    </div>

    <div class="card hero-panel">
      <div class="panel-header">
        <span class="hero-kicker">now</span>
        <span class="panel-status">available for select work</span>
      </div>

      <div class="panel-body">
        <div class="panel-row">
          <span class="panel-label">Focus</span>
          <span class="panel-value">Backend reliability & payment flows</span>
        </div>
        <div class="panel-row">
          <span class="panel-label">Current Stack</span>
          <span class="panel-value">Java · Spring Boot · OracleDB</span>
        </div>
        <div class="panel-row">
          <span class="panel-label">Research</span>
          <span class="panel-value">IIoT intrusion detection systems</span>
        </div>
      </div>

      <div class="panel-footer">
        <button id="heartBtn" aria-label="like" class="panel-like">
          <span id="heartIcon">♡</span>
          <span id="heartCount">0</span>
        </button>
        <span class="panel-note">tap to appreciate the work</span>
      </div>
    </div>
  </div>
</section>

<!-- HEART SCRIPT -->
<script>
  document.addEventListener("DOMContentLoaded", () => {
    const btn = document.getElementById("heartBtn");
    const icon = document.getElementById("heartIcon");
    const countEl = document.getElementById("heartCount");

    let count = Number(localStorage.getItem("hero-hearts") || 0);
    countEl.textContent = count;

    btn.addEventListener("click", () => {
      count++;
      localStorage.setItem("hero-hearts", count);
      countEl.textContent = count;

      icon.textContent = "♥";
      btn.classList.add("text-red-400");

      setTimeout(() => {
        icon.textContent = "♡";
      }, 300);
    });
  });
</script>

<style>
  .hero-grid {
    gap: 3rem;
  }

  .hero-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
  }

  .hero-panel {
    display: flex;
    flex-direction: column;
    gap: 1.4rem;
  }

  .panel-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .panel-status {
    font-size: 0.75rem;
    color: var(--text-dim);
    text-transform: uppercase;
    letter-spacing: 0.18em;
  }

  .panel-body {
    display: flex;
    flex-direction: column;
    gap: 0.9rem;
  }

  .panel-row {
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
  }

  .panel-label {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--text-dim);
  }

  .panel-value {
    color: var(--text-primary);
  }

  .panel-footer {
    display: flex;
    align-items: center;
    gap: 0.8rem;
  }

  .panel-like {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    border-radius: 999px;
    border: 1px solid var(--border-soft);
    padding: 0.3rem 0.8rem;
    color: var(--text-muted);
    transition: all 150ms ease;
  }

  .panel-like:hover {
    border-color: rgba(255, 255, 255, 0.3);
    color: #ff8b8b;
  }

  .panel-note {
    font-size: 0.75rem;
    color: var(--text-dim);
  }

  @media (min-width: 900px) {
    .hero-grid {
      display: grid;
      grid-template-columns: minmax(0, 1.1fr) minmax(0, 0.9fr);
      align-items: center;
    }
  }
</style>
