<section class="page-shell hero-shell">
  <div class="page-inner hero-grid">
    <div class="hero-block">
      <span class="hero-kicker">$ whoami</span>
      <h1 class="hero-title">Abinash S</h1>
      <p class="hero-subtitle">
        Software Engineer focused on backend systems, dependable architectures,
        and production-grade delivery across banking, web, and research
        projects.
      </p>

      <div class="hero-actions">
        <a href="/resume" class="btn-primary">View Resume</a>
        <a href="/service" class="btn-ghost">Services</a>
        <a href="/contact" class="btn-ghost">Contact</a>
      </div>

      <div class="hero-tags">
        <span class="tag">Java · Spring Boot · Microservices</span>
        <span class="tag">Payments · Transaction Systems</span>
        <span class="tag">ML Research · Cybersecurity</span>
      </div>
    </div>

    <div class="card hero-panel">
      <div class="panel-header">
        <span class="hero-kicker">now</span>
        <span class="panel-status status-pulse">available for select work</span>
      </div>

      <div class="panel-body">
        <div class="panel-row">
          <span class="panel-label">Focus</span>
          <span id="focusLine" class="panel-value">
            Backend reliability & payment flows
          </span>
        </div>
        <div class="panel-row">
          <span class="panel-label">Current Stack</span>
          <span class="panel-value">Java · Spring Boot · OracleDB</span>
        </div>
        <div class="panel-row">
          <span class="panel-label">Research</span>
          <span class="panel-value">IIoT intrusion detection systems</span>
        </div>
      </div>

      <div class="panel-footer">
        <button id="heartBtn" aria-label="like" class="panel-like">
          <span id="heartIcon">♡</span>
          <span id="heartCount">0</span>
        </button>
        <span class="panel-note">tap to appreciate the work</span>
      </div>
    </div>
  </div>
</section>

<!-- HEART SCRIPT -->
<script>
  document.addEventListener("DOMContentLoaded", () => {
    const btn = document.getElementById("heartBtn");
    const icon = document.getElementById("heartIcon");
    const countEl = document.getElementById("heartCount");
    const focusLine = document.getElementById("focusLine");

    let count = Number(localStorage.getItem("hero-hearts") || 0);
    countEl.textContent = count;

    btn.addEventListener("click", () => {
      count++;
      localStorage.setItem("hero-hearts", count);
      countEl.textContent = count;

      icon.textContent = "♥";
      btn.classList.add("text-red-400");

      setTimeout(() => {
        icon.textContent = "♡";
      }, 300);
    });

    if (focusLine) {
      const lines = [
        "Backend reliability & payment flows",
        "High-volume transaction systems",
        "Secure infrastructure & auditability",
      ];
      let idx = 0;
      setInterval(() => {
        idx = (idx + 1) % lines.length;
        focusLine.textContent = lines[idx];
      }, 6000);
    }
  });
</script>

<style>
  .hero-shell {
    min-height: calc(100vh - 240px);
    display: flex;
    align-items: center;
  }

  .hero-grid {
    gap: 3rem;
  }

  .hero-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.6rem;
  }

  .hero-panel {
    display: flex;
    flex-direction: column;
    gap: 1.4rem;
  }

  .panel-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .panel-status {
    font-size: 0.75rem;
    color: var(--text-dim);
    text-transform: uppercase;
    letter-spacing: 0.18em;
  }

  .status-pulse {
    position: relative;
  }

  .status-pulse::before {
    content: "";
    position: absolute;
    left: -12px;
    top: 50%;
    width: 6px;
    height: 6px;
    border-radius: 999px;
    background: var(--accent);
    transform: translateY(-50%);
    animation: pulse 2s ease-in-out infinite;
  }

  .panel-body {
    display: flex;
    flex-direction: column;
    gap: 0.9rem;
  }

  .panel-row {
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
  }

  .panel-label {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.2em;
    color: var(--text-dim);
  }

  .panel-value {
    color: var(--text-primary);
  }

  .panel-footer {
    display: flex;
    align-items: center;
    gap: 0.8rem;
  }

  .panel-like {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    border-radius: 999px;
    border: 1px solid var(--border-soft);
    padding: 0.3rem 0.8rem;
    color: var(--text-muted);
    transition: all 150ms ease;
  }

  .panel-like:hover {
    border-color: rgba(255, 255, 255, 0.3);
    color: #ff8b8b;
  }

  .panel-note {
    font-size: 0.75rem;
    color: var(--text-dim);
  }

  @media (min-width: 900px) {
    .hero-grid {
      display: grid;
      grid-template-columns: minmax(0, 1.1fr) minmax(0, 0.9fr);
      align-items: center;
    }
  }

  @keyframes pulse {
    0% {
      opacity: 0.3;
      box-shadow: 0 0 0 0 rgba(142, 242, 193, 0.4);
    }
    70% {
      opacity: 1;
      box-shadow: 0 0 0 8px rgba(142, 242, 193, 0);
    }
    100% {
      opacity: 0.3;
      box-shadow: 0 0 0 0 rgba(142, 242, 193, 0);
    }
  }
</style>
