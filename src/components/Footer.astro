<footer class="footer-shell">
  <div class="footer-orb"></div>
  <div class="footer-orb footer-orb-small"></div>

  <div class="footer-inner">
    <div class="footer-left">
      <span class="footer-kicker">system</span>
      <div class="footer-title">abx@portfolio</div>
      <p class="footer-subtitle">
        Premium terminal UI · keyboard-first · low-noise layout
      </p>
      <div class="footer-status">
        <span class="status-dot"></span>
        <span class="text-[var(--accent)]">operational</span>
        <span class="text-white/30">·</span>
        <span id="uptime" class="text-white/40">uptime 00:00:00</span>
      </div>
    </div>

    <div class="footer-nav">
      <div class="footer-nav-title">Navigate</div>
      <a href="/resume">Resume</a>
      <a href="/service">Service</a>
      <a href="/contact">Contact</a>
    </div>

    <div class="footer-right">
      <div class="footer-nav-title">Stack</div>
      <div class="footer-stack">
        <span>Astro</span>
        <span>·</span>
        <span>Bun</span>
      </div>
      <div class="footer-copy">© {new Date().getFullYear()} Abinash S</div>
    </div>
  </div>
</footer>

<style>
  .footer-shell {
    position: relative;
    overflow: hidden;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
    background: radial-gradient(circle at 20% 20%, rgba(30, 38, 60, 0.35), transparent 55%),
      radial-gradient(circle at 80% 30%, rgba(12, 14, 22, 0.9), transparent 50%),
      rgba(5, 6, 12, 0.9);
    backdrop-filter: blur(18px);
  }

  .footer-orb {
    position: absolute;
    inset: auto;
    width: 480px;
    height: 480px;
    right: -160px;
    top: -200px;
    border-radius: 999px;
    background: radial-gradient(circle, rgba(0, 0, 0, 0.9), rgba(20, 32, 58, 0.15) 55%, transparent 70%);
    box-shadow: inset 0 0 120px rgba(0, 0, 0, 0.8), 0 0 80px rgba(20, 32, 58, 0.45);
    opacity: 0.9;
  }

  .footer-orb-small {
    width: 220px;
    height: 220px;
    right: 30%;
    top: -80px;
    opacity: 0.6;
    filter: blur(1px);
  }

  .footer-inner {
    position: relative;
    z-index: 1;
    max-width: 1080px;
    margin: 0 auto;
    padding: 3.5rem 1.5rem 3rem;
    display: grid;
    gap: 2.5rem;
  }

  .footer-left {
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }

  .footer-kicker {
    text-transform: uppercase;
    letter-spacing: 0.2em;
    font-size: 0.7rem;
    color: var(--text-dim);
  }

  .footer-title {
    font-family: "Fraunces", serif;
    font-size: 1.6rem;
  }

  .footer-subtitle {
    color: var(--text-muted);
    max-width: 320px;
  }

  .footer-status {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    font-size: 0.85rem;
  }

  .status-dot {
    width: 8px;
    height: 8px;
    border-radius: 999px;
    background: var(--accent);
    box-shadow: 0 0 0 6px rgba(142, 242, 193, 0.12);
  }

  .footer-nav,
  .footer-right {
    display: flex;
    flex-direction: column;
    gap: 0.7rem;
  }

  .footer-nav-title {
    text-transform: uppercase;
    letter-spacing: 0.18em;
    font-size: 0.7rem;
    color: var(--text-dim);
  }

  .footer-nav a {
    color: var(--text-muted);
    transition: color 160ms ease;
  }

  .footer-nav a:hover {
    color: #fff;
  }

  .footer-stack {
    display: flex;
    gap: 0.6rem;
    color: var(--text-muted);
  }

  .footer-copy {
    color: var(--text-dim);
    font-size: 0.8rem;
  }

  @media (min-width: 860px) {
    .footer-inner {
      grid-template-columns: 1.2fr 0.6fr 0.6fr;
      align-items: center;
    }
  }
</style>

<!-- UPTIME SCRIPT -->
<script>
  const uptimeEl = document.getElementById("uptime");
  if (uptimeEl) {
    const start = Date.now();
    setInterval(() => {
      const diff = Date.now() - start;
      const h = String(Math.floor(diff / 3600000)).padStart(2, "0");
      const m = String(Math.floor((diff % 3600000) / 60000)).padStart(2, "0");
      const s = String(Math.floor((diff % 60000) / 1000)).padStart(2, "0");
      uptimeEl.textContent = `uptime ${h}:${m}:${s}`;
    }, 1000);
  }
</script>
