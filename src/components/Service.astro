<section class="page-shell">
  <div class="page-inner">
    <div class="hero-block">
      <span class="hero-kicker">$ services --list</span>
      <h1 class="hero-title">Services & Collaboration</h1>
      <p class="hero-subtitle">
        Professional services focused on engineering quality, clarity, and
        real-world outcomes.
      </p>
    </div>

    <div class="card-grid">
      <div class="card service-card">
        <h2 class="section-title">Resume</h2>
        <p class="section-subtitle">
          Career materials built for engineering roles with emphasis on impact
          and structure.
        </p>
        <ul class="service-list">
          <li>Resume build</li>
          <li>Resume review + ATS alignment</li>
        </ul>
        <div class="service-detail">
          <p>Includes impact framing, keywords, and role alignment.</p>
        </div>
      </div>

      <div class="card service-card">
        <h2 class="section-title">Collaboration</h2>
        <p class="section-subtitle">
          Architecture and implementation support for academic and early-stage
          projects.
        </p>
        <ul class="service-list">
          <li>Project collaboration</li>
          <li>System design guidance</li>
        </ul>
        <div class="service-detail">
          <p>Ideal for research prototypes, MVPs, and system planning.</p>
        </div>
      </div>

      <div class="card service-card">
        <h2 class="section-title">Development</h2>
        <p class="section-subtitle">
          Product-focused development across websites, APIs, and systems.
        </p>
        <ul class="service-list">
          <li>Website development</li>
          <li>API development</li>
          <li>System engineering</li>
        </ul>
        <div class="service-detail">
          <p>Delivery emphasizes performance, testing, and documentation.</p>
        </div>
      </div>
    </div>

    <div class="card service-cta">
      <div>
        <h3 class="section-title">Start a Project</h3>
        <p class="section-subtitle">
          Availability depends on scope and alignment. Reach out with your
          timeline and goals.
        </p>
      </div>
      <a href="/contact" class="btn-primary">Contact</a>
    </div>
  </div>
</section>

<style>
  .service-list {
    margin-top: 1.2rem;
    display: grid;
    gap: 0.6rem;
    color: var(--text-muted);
  }

  .service-list li {
    padding-left: 0.8rem;
    position: relative;
  }

  .service-list li::before {
    content: "•";
    position: absolute;
    left: 0;
    color: var(--accent);
  }

  .service-cta {
    display: flex;
    flex-direction: column;
    gap: 1.2rem;
    align-items: flex-start;
  }

  .service-card {
    position: relative;
    overflow: hidden;
    transition: transform 160ms ease, border-color 160ms ease;
  }

  .service-card:hover {
    transform: translateY(-4px);
    border-color: rgba(255, 255, 255, 0.2);
  }

  .service-detail {
    margin-top: 1rem;
    font-size: 0.85rem;
    color: var(--text-dim);
    opacity: 0;
    max-height: 0;
    overflow: hidden;
    transition: all 180ms ease;
  }

  .service-card:hover .service-detail {
    opacity: 1;
    max-height: 120px;
  }

  @media (min-width: 768px) {
    .service-cta {
      flex-direction: row;
      align-items: center;
      justify-content: space-between;
    }
  }
</style>
