---
interface Props {
  class?: string;
  narrow?: boolean;
}

const { class: className, narrow = false } = Astro.props;
---

<div class:list={["page-shell", narrow && "page-shell--narrow", className]}>
  <slot />
</div>