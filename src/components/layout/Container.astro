---
interface Props {
  class?: string;
  narrow?: boolean;
}

const { class: className, narrow = false } = Astro.props;
---

<div
  class:list={[
    "mx-auto w-full px-6 sm:px-8",
    narrow ? "max-w-[692px]" : "max-w-[1068px]",
    className,
  ]}
>
  <slot />
</div>